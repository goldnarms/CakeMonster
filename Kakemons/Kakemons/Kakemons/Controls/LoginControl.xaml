<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Kakemons.UI.Controls.LoginControl">
    <ContentView.Content>
        <Grid VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="60"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Label Text="Kakemons" Grid.Row="0" Grid.ColumnSpan="2"></Label>
            <Label Text="Kakemons" Grid.Row="1" Grid.ColumnSpan="2"></Label>
            <Button Text="Opprett ny bruker" Command="{Binding GoToCreateUserCommand}" Grid.Row="2" Grid.Column="0"></Button>
            <Button Text="Logg inn" Command="{Binding GoToLoginCommand}" Grid.Row="2" Grid.Column="1"></Button>
        </Grid>
    </ContentView.Content>
</ContentView>